
        document.getElementById('opt-reg').addEventListener('click', () => {
            window.scrollTo({
                top: window.innerHeight*2 + 240,
                behavior: 'smooth'
            })
        })